server {
    listen 80;
    listen [::]:80;

    server_name _;

    root /opt/awx-demo;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /health {
        default_type text/plain;
        return 200 "AWX Infrastructure Demo - OK\n";
    }
}
